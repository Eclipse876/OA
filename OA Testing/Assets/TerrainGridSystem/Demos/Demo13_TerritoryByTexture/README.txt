Demo Scene #13: shows how to use a texture to define territories.

Controls:
- Left click on a territory: hides it.
- Right click on a territory: draws its full frontier in yellow,
  and overlays the shared frontier with each neighbour in a distinct color, for 30s.
  Uses TerritoryGetFrontierVertices, rendered with LineRenderer (visible in Game view).

